local utils = require("utils")
local tui = require("tui")
local unpack = table.unpack

local M = {}

local route = {}

local function routine()
  local evdata = {os.pullEventRaw()}
  local route1 = evdata[1]
end

local function main()
  -- TODO
end